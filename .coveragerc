[run]
branch = True

source =
    freetx
    tests

omit =
    */_windows_libsecp256k1.py
    */test_bench.py

[paths]
source =
   freetx
   .tox/*/lib/python*/site-packages/freetx
   .tox/pypy*/site-packages/freetx

[report]
exclude_lines =
    no cov
    no qa

    # Ignore missing debug-only code
    def __repr__
    if self\.debug

    # Ignore non-runnable code
    if __name__ == .__main__.:
